#include <stdio.h>
#include <stdlib.h>

struct Node{
  char* name;
  struct Node *left;
  struct Node *right;
} BST;

void listAllEmployees(){
  int c;
  FILE *test;
  test = fopen("test.txt", "r");
  if(test){
    while((c = getc(test)) != EOF)
      putchar(c);
    fclose(test);
    printf("\n"
	   );
  }
}
void addEmployee(char[] name){
  printf("Employees need an id number, full name.\n");
  FILE *test = fopen("test.txt", "a");
  char name[] = name;
  fputs(name, text);
}

void deleteEmployee(){

}


void cleanEmployees(){

}

int main(){
  printf("welcome to the personal management system 2017:\n\t");
  printf("1.add employee\n2.remove employee\n3.list all employee names");
  int choice;
  scanf("%d", choice);
  printf(choice);
  return 0;
}
